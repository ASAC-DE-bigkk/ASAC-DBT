name: traffic
version: "1.0.0"
config-version: 2

profile: traffic

flags:
  require_certificate_validation: true
  static_parser: false

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  traffic:
    +materialized: table
