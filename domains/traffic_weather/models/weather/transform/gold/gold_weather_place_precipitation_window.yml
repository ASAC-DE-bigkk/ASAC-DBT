version: 2

models:
  - name: gold_weather_place_precipitation_window
    description: 장소별로 연속된 강수 형태 예보 시간을 묶은 공개 Gold 제품입니다. 실제 강수 관측이나 발생 보장이 아닙니다.
    access: public
    config:
      meta:
        serving:
          enabled: true
          external: true
          contract_version: v1
          product_id: weather_place_precipitation_window
          product_question: 특정 장소에서 비나 눈이 연속으로 예보된 시간대는 언제입니까?
          mcp_projection:
            schema_version: mcp-product-projection/v1
            operation:
              id: weather.find_precipitation_windows
              approval_status: approved
              execution_mode: catalog_only
            question_examples:
              - 서울역에서 비나 눈이 이어질 것으로 예보된 시간대는 언제인가요?
              - 오늘 여의도공원에 강수 형태가 예보된 연속 구간이 있나요?
              - 이 장소의 다음 강수 예보 시작과 종료 시각은 언제인가요?
          grain: place_id와 window_start_at마다 한 행 — 명시적으로 강수 형태가 예보된 연속 시간 구간.
          primary_key: [product_row_id]
          publication_mode: snapshot
          zero_policy: allow
          publication_trigger:
            trigger_type: asset
            max_interval_minutes: 240
          event_time: window_start_at
          freshness_slo_minutes: 240
          retention_or_horizon: 최신 KMA 예보가 제공하는 미래 구간만 매 게시 시점에 교체합니다.
          shape: event
          source_evidence:
            - source_id: kma_vilage_fcst
              source_url: https://www.data.go.kr/data/15084084/openapi.do
              license: 공공누리 제1유형(출처표시)
              license_url: https://www.kogl.or.kr/info/licenseType1.do
              redistribution: allowed_with_attribution
              attribution: 기상청
              rights_checked_at: "2026-08-06"
          public_projection:
            schema_version: "1.0.0"
            columns:
              - product_row_id
              - place_id
              - window_start_at
              - window_end_at
    columns:
      - name: product_row_id
        description: place_id와 강수 구간 시작시각을 직렬화한 식별자입니다.
        data_type: varchar
        tests: [not_null, unique]
        config:
          meta:
            semantic_role: primary_key
            nullable: false
            null_meaning: product_row_id 값이 없는 행은 이 공개 제품 계약에서 허용하지 않습니다.
            unit: not_applicable
      - name: place_id
        description: 장소 식별자입니다.
        data_type: varchar
        tests: [not_null]
        config:
          meta:
            semantic_role: join_key
            nullable: false
            null_meaning: place_id 값이 없는 행은 이 공개 제품 계약에서 허용하지 않습니다.
            unit: not_applicable
      - name: window_start_at
        description: 강수 형태 예보가 연속되기 시작하는 KST 시각입니다.
        data_type: timestamp(6)
        tests: [not_null]
        config:
          meta:
            semantic_role: timestamp
            nullable: false
            null_meaning: window_start_at 값이 없는 행은 이 공개 제품 계약에서 허용하지 않습니다.
            unit: not_applicable
      - name: window_end_at
        description: 강수 형태 예보가 연속되는 마지막 KST 시각입니다.
        data_type: timestamp(6)
        tests: [not_null]
        config:
          meta:
            semantic_role: timestamp
            nullable: false
            null_meaning: window_end_at 값이 없는 행은 이 공개 제품 계약에서 허용하지 않습니다.
            unit: not_applicable
