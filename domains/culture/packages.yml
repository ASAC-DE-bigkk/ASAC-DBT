packages:
  - local: ../../packages/asac_axes
  # 서빙 계약(#346): 복합 primary_key 의 고유성 근거 — serving_contract validator 가
  # 이름의 unique_combination 으로 인정(fixture 정본도 dbt_utils 사용).
  - package: dbt-labs/dbt_utils
    version: 1.3.1
